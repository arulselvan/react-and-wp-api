import React from 'react';

const header=()=>{
    return(
        <header className="Header">
          <h1 className="App-title"> Learning React With Wordpress API</h1>
        </header>
    )
}

export default header;